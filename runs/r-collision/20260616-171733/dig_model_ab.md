# Dig-quality A/B — Sonnet vs DeepSeek vs Haiku (revise step)

Source run: `20260616-171733` · 6 real contexts · same input, three models. **You judge the nuance.**

Read each block for: did it PRESERVE the cross-domain vocabulary and sharpen the analogy (good), or SAND it into generic prose (the failure mode the prompt warns against)?


---

## Context 1 — domain: `film`  (report wander-S589f28-001)

**Original iteration-1 (what's being revised):**
> Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its core architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts between agents rather than free-text handoffs. The pipeline stages — research, st


### SONNET (baseline, removed)  ($0.0169)
- **exploration:** Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its three load-bearing architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts as inter-agent contracts rather than free-text handoffs. The pipeline stages — research, structure, script, casting, set, director, storyboard, animator, continuity, editor — are fixed; what varies per genre is what each stage's typed artifact *means*, encoded in a genre template that acts as a schema overlay on the universal pipeline.
- **advancement:** The sharpest cross-world bridge: Showrunner's typed artifact membrane is a concrete implementation of bounded reversible bias injection — and the genre-template schema diff is a concrete implementation of structural gap signaling. When a gap-chasing agent distorts its framing to pursue a blind spot, the distortion is bounded because what it emits is a typed artifact; the downstream assembly stage operates on a schema-validated signal, not on the raw framing the agent was running under. This is the mechanism layer's requirement made physical: bias cannot accumulate into permanent structural distortion because the type is the membrane. The genre-template schema diff goes further: when Showrunner instantiates a new genre, the delta between the universal pipeline's artifact schemas and the genre template's overrides is precisely the set of structural slots the pipeline does not yet know how to fill — which is structurally identical to the anchor's gap-ranker reading the skeleton's missing pieces. The 'frames before video' principle maps onto the anchor's 'resolve gap-type ambiguity at the attempt-history layer before committing allocation resources': commit to a cheap, verifiable representation layer first, promote to expensive downstream only after correctness is confirmed.
- **what_does_not_map:** Showrunner's pipeline is strictly acyclic — no downstream stage can re-open an upstream one, and there is no attempt-history accumulator that distinguishes 'unfillable gap' (stage structurally cannot produce this artifact) from 'under-attempted gap' (stage failed this run but would succeed with a different input framing). The anchor's orchestrator must hold both signals simultaneously to make dispatch decisions; Showrunner's typed artifact membrane that makes bias bounded also makes failure opaque — a stage either emits a valid artifact or errors out, and the pipeline has no mechanism to fold that failure signal back into a revised allocation strategy for the next cycle.


### DEEPSEEK (new workhorse)  ($0.0064)
- **exploration:** Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its core architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts between agents rather than free-text handoffs. The pipeline stages — research, structure, script, casting, set, director, storyboard, animator, continuity, editor — are fixed; what varies per genre is what each stage's typed artifact *means*.
- **advancement:** Showrunner's typed artifact membrane (e.g., a Script object with required fields) directly enforces bounded reversible bias injection. When a cycle orchestrator sends agents to chase a structural blind spot like 'missing conflict in scene 2', the agent may distort its framing to hunt for conflict, but it must produce a valid Script artifact; downstream assembly sees only the schema-validated fields, not the internal framing. The distortion is bounded and reversible because the artifact's type acts as a normalization filter — exactly the property the mechanism layer demands to prevent accumulated drift. Similarly, Showrunner's 'frames as source-of-truth, video as cheap interpolation' principle implements convergence by saturation not exhaustion: iteration happens at the storyboard frame level where changes are cheap and correctness is verifiable, analogous to the orchestrator's attempt-history discriminator that tests whether the cheap-attempt layer has been saturated before deciding if a gap is unfillable. The typed artifact as inter-agent contract is the concrete implementation of the anchor's need to fuse independent gap-detectors: schema fields encode the expected shape of a completed draft, so a missing required field is simultaneously a goal-blind structural gap and a goal-aware auditor signal — a natural dual-signal fusion point.
- **what_does_not_map:** Showrunner's pipeline is irreversible: downstream stages never reopen upstream ones, so the system cannot reallocate agents based on gap-failure evidence. The anchor's cycle orchestrator must hold divergence and convergence simultaneously, allowing a later cycle to re-frame a lead discovered earlier and send fresh agents — Showrunner has no mechanism for this. There is no dispatcher that varies agent count based on gap structure; each stage runs with a fixed single-agent model. Crucially, Showrunner lacks an attempt-history discriminator: it either produces a valid typed artifact or halts, never recording that a stage was attempted multiple times with different framings and came back empty, so it cannot distinguish an unfillable gap from an under-attempted one — the missing signal that would tell the orchestrator to stop versus to re-command deeper.


### HAIKU (new nuance)  ($0.0070)
- **exploration:** Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its core architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts between agents rather than free-text handoffs. The pipeline stages — research, structure, script, casting, set, director, storyboard, animator, continuity, editor — are fixed; what varies per genre is what each stage's typed artifact *means*. Critically: the continuity stage runs *after* animation and *before* edit, detecting structural collisions in the rendered output and feeding corrections back upstream to director and storyboard — a bounded re-entry loop that does not restart the pipeline.
- **advancement:** The structural bridge: Showrunner's typed artifact as inter-agent contract is a concrete implementation of bounded reversible bias injection. When a gap-chasing agent distorts its framing to pursue a structural blind spot, the distortion is bounded because what it emits is a typed artifact — the downstream assembly stage cannot be contaminated by the framing distortion because the type is the membrane. This directly resonates with the mechanism layer's requirement that bias injected during gap-chasing must not accumulate into permanent structural distortion. Typed handoffs enforce that the re-integration step works on a normalized, schema-validated signal rather than on the raw framing the agent was operating under. The continuity stage's re-entry loop is the key: it detects collision-type gaps (negative topology — structural impossibilities in the rendered skeleton) and routes corrections back to specific upstream stages without restarting the whole pipeline. This is the allocation brain's unfillable-versus-under-attempted discriminator in concrete form: if continuity detects a collision that director and storyboard cannot resolve with bounded re-attempts, the gap is unfillable and the loop halts; if corrections propagate and collisions clear, the gap was under-attempted. The frames-as-source-of-truth principle also maps to the anchor's convergence-by-saturation pattern: the system commits to a cheap, verifiable representation layer before promoting to expensive downstream stages, which is structurally identical to resolving gap-type ambiguity at the attempt-history layer before committing fresh allocation.
- **what_does_not_map:** Showrunner's re-entry loop is triggered by a *single, deterministic detector* (continuity stage) running on a *fixed representation* (rendered frames). The anchor requires *dual-signal fusion*: a goal-blind structural piece-finder and a goal-aware auditor must be fused into one dispatch priority, and the fusion itself is the hard problem — Showrunner has no analogue for this. Additionally, Showrunner's typed artifact schema is *per-stage*, not *per-gap*: the schema does not vary based on which structural blind spot was just exposed. The anchor's allocation brain must convert goal-relative blind spots into goal-free exploration leads — a reframing operation — and then decide agent count from the gap structure itself. Showrunner allocates agents per stage, not per gap-type. Finally, Showrunner's attempt-history is implicit (a stage either produces a typed artifact or fails); the anchor requires an explicit attempt-history discriminator that reads *whether agents were actually sent at a gap and came back empty* to distinguish unfillable from under-attempted. Showrunner has no mechanism for this reading.


---

## Context 2 — domain: `relationships`  (report wander-S4f31ec-002)

**Original iteration-1 (what's being revised):**
> Stigmergy's ART-based competitive mesh offers a mature, theoretically grounded implementation of several mechanism-layer nodes: signals route via BFS stop-on-first-accept (satisficing under resource constraint), workers fork/merge/decay by familiarity threshold (convergence by saturation), and the vigilance parameter governs how aggressively new categories form versus reinforcing old ones (bounded


### SONNET (baseline, removed)  ($0.0174)
- **exploration:** Stigmergy's ART-based competitive mesh offers a mature, theoretically grounded implementation of several mechanism-layer nodes: signals route via BFS stop-on-first-accept (satisficing under resource constraint), workers fork/merge/decay by familiarity threshold (convergence by saturation), and the vigilance parameter governs how aggressively new categories form versus reinforcing old ones (bounded reversible bias injection). The mechanism layer matched 7/7, the highest structural fidelity in this room. The sharpest concrete find: ART's 'complement coding' — where full workers raise vigilance thresholds to prevent category monopoly — is a working implementation of goal-neutralization-of-targeted-leads: the system structurally prevents any single high-salience pattern from consuming all routing capacity by encoding both a signal AND its complement, so the mesh can only accept a new input into an existing category if it matches on both dimensions simultaneously, making monopoly geometrically impossible rather than merely discouraged by policy.
- **advancement:** The cross-world analogy sharpens at one specific joint: the anchor's 'how much goal-awareness can the dispatch signal carry before the structural finder's output degrades' maps onto ART's vigilance parameter as a formal knob — low vigilance lets goal-biased signals coarsen the category mesh (attractor hazard: the whole skeleton collapses toward the auditor's prior), high vigilance forces new categories to form for every biased input (category explosion: the skeleton never closes). The anchor's dual-detector fusion problem is therefore not a policy question but a vigilance-setting problem: the contamination threshold is the vigilance value above which the goal-aware auditor's dispatch signal can no longer distort the structural finder's category boundaries, and complement coding is the mechanism that enforces it structurally rather than by runtime monitoring.
- **what_does_not_map:** ART has no attempt-history discriminator: it cannot distinguish 'this category is empty because no matching signal exists in the world' from 'this category is empty because the vigilance setting rejected every candidate that arrived' — stale categories decay by age (exhaustion-based halting), not by confirmed-attempt-count (saturation-based halting), which is precisely the anchor's unfillable-gap versus under-attempted-gap distinction. The anchor's dual gap-detector architecture — one detector structurally goal-blind, one goal-aware — also has no counterpart: ART's mesh is uniformly goal-blind, so the contamination-prevention guarantee complement coding provides is a guarantee against internal monopoly, not against an external goal-aware signal injecting systematic directional bias into the category formation process.


### DEEPSEEK (new workhorse)  ($0.0043)
- **exploration:** Stigmergy's ART-based competitive mesh remains the highest-fidelity match to the mechanism layer, mapping 7/7 nodes: BFS stop-on-first-accept (satisficing under resource constraint), familiarity-threshold fork/merge/decay (convergence by saturation), and vigilance-gated plasticity (bounded reversible bias injection). The cross-world analogy sharpens concretely: ART's complement coding raises vigilance thresholds across all competing categories whenever a category becomes over-active, structurally preventing any single pattern from consuming all routing capacity. This is a precise mechanism-level analogue of 'goal-neutralization of targeted leads' — it is not a policy but an architectural guarantee that a high-priority gap cannot monopolise the mesh, exactly as the anchor needs to stop a gap-chasing wave from starving the wandering wave.
- **advancement:** ART's vigilance-gated plasticity and complement coding together constitute a proven mathematical design pattern for holding the divergence-convergence tension while fusing two signal streams without contamination. The vigilance parameter operates as a tunable knob that limits the bias the gap-chaser can inject before the system refuses to update, directly implementing the 'bias budget' the cycle orchestrator must enforce. Complement coding then acts as a structural guarantee that the goal-aware auditor's priority signal does not drown out the goal-blind structural finder's unbiased pattern formation, implementing 'dual-signal fusion without contamination' at the architectural level.
- **what_does_not_map:** Stigmergy explicitly lacks an attempt-history discriminator: it cannot distinguish 'this gap is empty because nothing exists' from 'this gap is empty because I routed wrong' — stale patterns simply decay, which is exhaustion-based halting, not saturation-based. More critically, the anchor's dual gap-detector fusion (a goal-blind structural piece-finder and a goal-aware auditor that knows what the draft needs) has no counterpart: stigmergy's mesh is uniformly goal-blind, surfacing patterns from signal statistics with no second detector. The anchor's cycle-level 'twisting of framing toward a gap' — a deliberate re-orientation of the entire exploring system — has no analogue because the mesh does not reorient; it only reinforces or decays existing categories.


### HAIKU (new nuance)  ($0.0054)
- **exploration:** Stigmergy's ART-based competitive mesh implements the mechanism layer's core nodes with high fidelity: signals route via BFS stop-on-first-accept (satisficing under resource constraint), workers fork/merge/decay by familiarity threshold (convergence by saturation), and vigilance governs plasticity-stability tradeoff (bounded reversible bias injection). The mechanism layer matched 7/7. The sharper find: ART's complement coding — where full workers raise vigilance thresholds to prevent category monopoly — is a structural implementation of 'goal-neutralization of targeted leads'. It prevents any single high-priority gap from consuming all routing capacity by making the system itself resistant to over-specialization, not by policy but by architecture.
- **advancement:** Complement coding directly solves the divergence-convergence tension the anchor names: new patterns stay open (plasticity) while stable ones resist distortion (stability), and the vigilance parameter is the tunable knob governing how much bias a gap-chasing cycle injects before the system refuses to update. This maps onto the mechanism layer's 'recursive self-amendment loop' where bias must be bounded to keep the loop stable. More precisely: if the auditor (goal-aware) signals a high-priority gap with high confidence, complement coding prevents that signal from monopolizing the next agent wave's routing — the system structurally refuses to let goal-awareness collapse divergence.
- **what_does_not_map:** ART has no attempt-history discriminator: it cannot distinguish 'this gap is empty because nothing exists' from 'this gap is empty because I routed wrong'. Stale patterns decay by age (exhaustion-based halting), not by saturation. Critically, the anchor requires dual gap-detector fusion (goal-blind structural finder + goal-aware auditor) with contamination prevention. Stigmergy's mesh is uniformly goal-blind — it surfaces patterns from signal statistics alone. There is no second detector that knows what the draft needs, and no formal mechanism preventing the goal-aware auditor's output from biasing the goal-blind finder into false structural conclusions. Complement coding prevents monopoly, but does not prevent systematic contamination of one detector by the other.


---

## Context 3 — domain: `biographies`  (report wander-S63728f-001)

**Original iteration-1 (what's being revised):**
> The abryfs/organism source is a prompt-engineering coordination protocol for founder-engineers, not a self-amending research loop. Its nine 'organs' participate in or lead task flows via a fixed 7-step protocol, with tiered visibility and named pitfall detection. The high node-match count reflects structural vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the und


### SONNET (baseline, removed)  ($0.0162)
- **exploration:** The abryfs/organism source is a human-steered coordination protocol for founder-engineers: nine named organs participate in a fixed 7-step task flow, with tiered visibility, pitfall detection, and gate-override logging. The high node-match count reflects genuine structural vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the underlying architecture is a human-triggered workflow, not an autonomous cycle that rewrites its own framing between iterations. The closest structural analog to the anchor's cycle orchestrator is the combination of 'Eyes' (inward bias-detector) plus the gate-override log, but even together they do not constitute a self-amending loop.
- **advancement:** The 'Eyes' organ introduces a structurally distinct pattern worth preserving: a single agent whose explicit mandate is to surface what the system itself cannot see — cognitive biases, self-deception, motivated reasoning — operating simultaneously inward (against the founder's own reasoning) and outward (against the environment). This is a concrete instantiation of the anchor's 'dual-signal fusion without contamination' problem: Eyes must stay goal-blind enough to catch motivated reasoning while remaining goal-aware enough to know which biases matter for this decision. More precisely, the 100-pitfall layer — each pitfall firing as Block/Warn/Note from attempt history — is a working prototype of the anchor's 'attempt-history as gap-type discriminator': the system distinguishes 'gap exists because the pattern is structurally absent' from 'gap exists because the founder is actively avoiding it', and that distinction is only readable from the history of prior gate overrides. The anchor's UNFILLABLE vs. UNDER-ATTEMPTED gap discrimination is exactly this problem, but the anchor must solve it without a human reading the override log — the log itself must become a signal the orchestrator consumes.
- **what_does_not_map:** The specific mismatch is this: in organism, the gate-override log is observational — it records founder decisions for later human review, but does not feed back into pitfall thresholds or alter the next cycle's sampling bias. In the anchor, the equivalent log must be a live input to the gap-ranker: the orchestrator must read 'agents were dispatched at this gap and returned empty' as a different signal than 'this gap was never targeted', and that distinction must alter dispatch priority autonomously. Organism has the data structure but not the feedback arc; the anchor requires the feedback arc as a first-class architectural component, not an audit trail.


### DEEPSEEK (new workhorse)  ($0.0045)
- **exploration:** The abryfs/organism source is a prompt-engineering coordination protocol for founder-engineers, not a self-amending research loop. Its nine 'organs' participate in or lead task flows via a fixed 7-step protocol, with tiered visibility and named pitfall detection. The high node-match count reflects structural vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the underlying architecture is a human-steered, task-triggered workflow, not an autonomous cycle that rewrites its own framing between iterations.
- **advancement:** The source's 'Eyes' organ introduces a structurally distinct pattern not yet on the board: a single agent whose explicit mandate is to surface what the system itself cannot see — cognitive biases, self-deception, drift — operating inward (against the founder's own reasoning) and outward (against the environment) simultaneously. This resonates with the anchor's 'dual-signal fusion without contamination' at a meta level: Eyes must remain goal-blind enough to catch motivated reasoning while being goal-aware enough to know which biases matter for this decision. The 100-pitfall layer — each firing as Block/Warn/Note from attempt history — also resonates with the mechanism layer's 'attempt-history as gap-type discriminator': the system distinguishes 'gap exists because pattern is absent' from 'gap exists because founder is avoiding it', and that distinction is only readable from the history of prior gate overrides. The cross-world analogy crystallizes here: organism's gate-override log IS an attempt-history, and the pitfall severity escalation (Note→Warn→Block) IS a gap-type discriminator — but one that operates on founder psychology rather than structural completeness. The anchor's orchestrator needs to read 'was this gap ever actually chased?' from agent dispatch logs; organism reads 'was this pitfall ever actually overridden?' from gate logs. Both systems face the same negative topology problem: distinguishing an attractor hazard (the founder/agent keeps circling back to a comfortable dead end) from a genuine unfilled structural cavity, using only the shape of past attempts.
- **what_does_not_map:** The anchor's loop is autonomous and self-amending: the orchestrator temporarily distorts its own framing, then re-integrates without human steering, and the halting criterion is structural completeness the system itself evaluates. Organism's loop is neither autonomous nor self-amending in this sense — a human founder triggers every cycle, confirms or overrides every gate, and sets the focus that all organs serve. There is no mechanism by which organism detects that its own framing has drifted and corrects it without founder input; 'Eyes' flags drift to the founder, it does not re-integrate the distortion internally. The convergence signal is also fundamentally different: organism converges when the founder decides to ship, not when a structural completeness checkpoint is satisfied by the system itself. Specifically, organism's halting criterion is external (founder judgment), while the anchor's is internal (skeleton meets checkpoint OR marginal returns collapse). This is not a generic hedge — it's a specific architectural mismatch: organism has no self-evaluating convergence function, only a human-triggered one.


### HAIKU (new nuance)  ($0.0057)
- **exploration:** The abryfs/organism source is a human-steered task-coordination protocol, not an autonomous self-amending cycle. Its nine organs participate in fixed 7-step workflows via tiered visibility and named pitfall detection. The high node-match count reflects vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the underlying architecture is trigger-responsive (founder initiates each cycle), not self-diagnosing (system detects its own structural incompleteness and re-allocates without human gate).
- **advancement:** The 'Eyes' organ introduces a structurally distinct pattern: a single agent mandated to surface what the system cannot see — cognitive biases, motivated reasoning, environmental drift — operating inward and outward simultaneously while remaining goal-blind enough to catch self-deception yet goal-aware enough to know which biases matter. This maps directly to the anchor's 'fuse two independent gap-detectors (goal-blind structural piece-finder and goal-aware auditor) into one dispatch priority' — Eyes performs exactly this fusion at the meta level. The gate-override log ('patterns reveal where judgment needs adjusting') also maps to the anchor's 'attempt-history as gap-type discriminator': organism distinguishes 'gap exists because pattern is absent' from 'gap exists because founder is avoiding it' by reading prior override sequences, which is readable from whether gates were actually triggered and what founder chose. This is the closest analog to the anchor's 'unfillable gap (stop) versus under-attempted gap (re-command deeper)' distinction.
- **what_does_not_map:** The anchor's cycle orchestrator must autonomously decide whether to re-command a gap-chase or halt — it reads agent-return emptiness and converts that into a halting decision without human confirmation. Organism has no equivalent: when Eyes flags a blind spot or a gate fires, the founder decides whether to override, iterate, or ship. The founder owns the halt decision; the system does not. Additionally, the anchor's 'temporarily twist framing toward an identified gap, chase it, fold what comes back into the skeleton' requires the orchestrator to inject bias into the next wave's sampling strategy, then de-bias the result during re-integration — all internally. Organism's pitfall layer warns the founder of bias but does not inject it strategically into task design, nor does it have a mechanism to de-bias findings after a biased search. The convergence checkpoint is also fundamentally different: organism converges when the founder ships (external decision); the anchor converges when the skeleton satisfies a structural completeness criterion the system itself evaluates (internal decision).


---

## Context 4 — domain: `military_strategy`  (report wander-Seb9414-002)

**Original iteration-1 (what's being revised):**
> Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but the rest of the pipeline is a fixed assembly line, not a 


### SONNET (baseline, removed)  ($0.0153)
- **exploration:** Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but the rest of the pipeline is a fixed assembly line, not a self-amending loop. The pipeline knows its own shape before the first frame is rendered; the anchor's system must discover its shape by repeatedly exposing its own ignorance.
- **advancement:** The hybrid compute routing — sending tasks to cloud vs. local RTX 5090 based on capability requirements — is a concrete instantiation of 'allocation from structural topology': Showspring does not assign work by queue position but by the nature of the task, which is structurally parallel to the anchor's gap-ranker dispatching agents toward highest-priority structural holes rather than round-robin. The sharper analogy, however, is negative: the Creative Director's persona-plus-judge is a one-shot tournament that collapses divergence into a single winner and then exits — it has no re-entry path, no attempt-history, and no mechanism to distinguish 'this gap is unfillable' from 'no agent was aimed at it yet.' That absence is precisely the attractor hazard the anchor must engineer around: a system that mistakes low-attempt-count for genuine emptiness will halt prematurely on the wrong convergence signal.
- **what_does_not_map:** The anchor's recursive self-amendment loop — where the structure diagnoses its own incompleteness, that diagnosis generates a biased sampling strategy, and the bias must be bounded so distortion does not accumulate across cycles — has no counterpart anywhere in Showspring. Showspring's halting criterion is completion of a fixed sequence, not saturation of a structural completeness checkpoint; there is no diminishing-returns doubt meter, no gap-namer, and no mechanism for the pipeline to temporarily twist its own framing and then re-integrate without letting that twist permanently corrupt shared state.


### DEEPSEEK (new workhorse)  ($0.0101)
- **exploration:** Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). If we map the anchor's skeleton concept onto Showspring, the skeleton is the entire 24-step sequence, fully known from the start; a gap-detector would find zero structural gaps, and the cycle orchestrator would immediately halt without ever entering a self-amending loop. The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but it is a one-shot tournament, not a recursive loop that temporarily distorts framing and must re-integrate without letting distortion accumulate.
- **advancement:** The hybrid compute routing — sending tasks to cloud vs. local RTX 5090 based on capability requirements — resonates with the mechanism layer's 'resource allocation from structural topology': Showspring allocates compute not by queue position but by the nature of the task, structurally parallel to the anchor's dynamic agent dispatch toward highest-priority gaps rather than round-robin assignment. The Cooperative File Locks preventing conflicting git states across simultaneous AI engineering agents also weakly resonates with 'bounded reversible bias injection' — a mechanism for ensuring that parallel agents do not permanently corrupt shared state, analogous to the anchor's requirement that bias injected during gap-chasing be reversible so the skeleton can be reassembled without distortion.
- **what_does_not_map:** The anchor's core tension — a system that does not know its own shape, must expose its own ignorance, and self-amends its framing across cycles — is entirely absent. Showspring's skeleton is fully known a priori, so the anchor's gap-detection, attempt-history discrimination (distinguishing 'nothing exists here' from 'I have not looked right'), and halting criterion based on structural completeness are inapplicable. The pipeline halts because it exhausts its fixed steps, not because it saturates. The Creative Director's persona-plus-judge is a one-shot tournament, not a recursive self-amendment loop that temporarily distorts framing and must re-integrate without letting distortion accumulate.


### HAIKU (new nuance)  ($0.0059)
- **exploration:** Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but the rest of the pipeline is a fixed assembly line, not a self-amending loop. Critically: Showspring's compute routing (cloud vs. local RTX 5090 by task capability) and Cooperative File Locks (preventing conflicting git states across parallel agents) are its only mechanisms for adaptive allocation — and both operate *within* a pre-determined skeleton, never reshaping the skeleton itself.
- **advancement:** The hybrid compute routing resonates sharply with the anchor's 'allocation brain': Showspring dispatches tasks to compute resources based on structural capability requirements (a video upscaler needs GPU; a metadata writer does not), not queue order. This is isomorphic to the cycle orchestrator's dispatch priority — sending agent waves toward gaps ranked by structural necessity, not by arrival time. The Cooperative File Locks also instantiate a bounded reversible bias mechanism: parallel agents can write to shared git state simultaneously, but locks prevent permanent corruption of the skeleton. In the anchor's language, this is a *collision-avoidance topology* — agents can diverge (write in parallel) only if their writes are mutually reversible. If one agent's amendment corrupts the shared structure, the lock prevents that corruption from cascading to the next cycle.
- **what_does_not_map:** Showspring has no gap-detector, no auditor, and no attempt-history discriminator. The anchor must distinguish 'this gap is unfillable' from 'this gap has been under-attempted' by reading whether agents were actually sent at it and came back empty. Showspring cannot do this: a missing step in the pipeline is simply not executed; there is no mechanism to detect that a step *should* exist, send agents to find it, and then decide whether silence means 'nothing is there' or 'I did not search correctly.' The Creative Director's persona-plus-judge is a one-shot tournament within a single step, not a recursive self-amendment loop. Showspring's halting criterion is deterministic (24 steps complete); the anchor's is structural (skeleton meets checkpoint OR diminishing returns collapse). Showspring does not re-route its own framing mid-run based on what it discovers about its own incompleteness.


---

## Context 5 — domain: `sociology`  (report wander-S4f31ec-001)

**Original iteration-1 (what's being revised):**
> Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a p


### SONNET (baseline, removed)  ($0.0168)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly — and crucially, the ART vigilance parameter acts as a tunable resonance threshold that determines when a signal is 'close enough' to an existing category versus when it forces a new category open.
- **advancement:** The sharpest cross-world mapping is between ART's vigilance parameter and the anchor's unfillable-vs-under-attempted discriminator. In ART, when no existing category resonates above the vigilance threshold after repeated presentation of a signal, the system is forced to instantiate a new category — this is structurally identical to the anchor's heuristic that 'agents were sent at this gap and came back empty' should trigger reclassification from under-attempted to unfillable. The vigilance parameter also offers a concrete fusion mechanism for the dual gap-detectors: the goal-blind structural finder sets a low vigilance (accepts loose matches, stays open), the goal-aware auditor sets a high vigilance (demands tight fit to draft requirements), and dispatch priority is read from the gap between their verdicts — a signal that resonates for the blind finder but not the auditor is a strong dispatch candidate, while a signal that resonates for neither is the unfillable-gap signal. The stigmergic decay mechanism then bounds the bias injection problem: reinforcing active workers amplifies promising leads, but time-decay prevents any locally resonant pattern from permanently occluding novel signals — a concrete implementation shape for 'bounded reversible bias injection' that the anchor's mechanism layer requires.
- **what_does_not_map:** Stigmergy's workers have no target structure and no per-gap attempt-count — decay is time-based, not attempt-count-based, so the anchor's halting discriminator ('was this gap actually attempted and returned empty, or merely never reached?') cannot be read from stigmergy's state without grafting on an explicit attempt-history register that stigmergy does not natively carry. The anchor's skeleton-completeness checkpoint also has no analog: stigmergy surfaces patterns as side-effects of routing, not as components being assembled toward a structural criterion, so the convergence signal in stigmergy (pattern stabilization) is not the same kind of object as the anchor's 'skeleton satisfies checkpoint.'


### DEEPSEEK (new workhorse)  ($0.0055)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly.
- **advancement:** This resonates with the mechanism layer's 'convergence by saturation not exhaustion' and the essence layer's 'productive ignorance exposure': the ART-based mesh does not hunt for gaps by explicit enumeration — it lets unmatched signals accumulate until resonance fails and a new category is forced open, which is structurally identical to the anchor's 'this gap is empty because I have not yet looked in the right way' discriminator operating through attempt-history pressure rather than explicit gap-typing. The stigmergic routing model (stop-on-first-accept, decay of stale patterns) also offers a concrete implementation shape for 'bounded reversible bias injection': bias is injected by reinforcing active workers, but the decay mechanism bounds accumulation and prevents a locally resonant pattern from permanently occluding novel signals. The vigilance parameter — the threshold controlling how much mismatch a system tolerates before declaring a new category — maps directly onto the anchor's gap-severity threshold: a low-vigilance stigmergy absorbs diverse signals into few workers (premature convergence), while a high-vigilance stigmergy fragments into too many workers (divergence without closure), which is the exact tension the cycle orchestrator must navigate when fusing two gap-detectors into one dispatch priority.
- **what_does_not_map:** Stigmergy has no cycle orchestrator and no structural skeleton — it is a continuous, acyclic signal processor that surfaces patterns as side-effects of routing, not as components of a draft being assembled toward a completeness criterion. The anchor's halting problem ('is this gap unfillable or under-attempted?') requires a history of directed attempts against a known structural target; stigmergy's workers have no target structure and no attempt-history per gap — decay is time-based, not attempt-count-based, so the discrimination the anchor needs cannot be read from stigmergy's state. The stigmergic mesh cannot distinguish between a gap that has never been probed and a gap that has been probed repeatedly and returned empty, because its state only encodes recency-weighted resonance strength, not directed-attempt counts.


### HAIKU (new nuance)  ($0.0055)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly.
- **advancement:** The ART vigilance parameter is a formal analog to the anchor's gap-severity threshold: it controls whether a mismatched signal forces a new category (high vigilance = low tolerance for deviation = new gap forced open) or reinforces an existing one (low vigilance = high tolerance = existing structure absorbs the signal). This directly addresses the cycle orchestrator's fusion problem: the two gap-detectors (goal-blind structural finder and goal-aware auditor) can operate at different vigilance settings without contaminating each other — the structural finder runs high vigilance (forces new categories aggressively, detects structural voids), the auditor runs lower vigilance (accepts signals that fit the draft's current shape, detects what the skeleton is missing). The decay mechanism also offers a concrete implementation for 'bounded reversible bias injection': bias is injected by reinforcing active workers, but time-based decay prevents a locally resonant pattern from permanently occluding novel signals — this is structurally identical to the anchor's need to distinguish unfillable gaps (no agent attempts return signal → decay to silence) from under-attempted ones (agent attempts return signal → reinforcement, not decay).
- **what_does_not_map:** Stigmergy's decay is time-based, not attempt-count-based. The anchor's halting discriminator ('is this gap unfillable or under-attempted?') requires reading attempt-history pressure against a known structural target — it needs to know that agents WERE sent at a gap and came back empty, which is a directed, bounded, countable event. Stigmergy's workers have no target structure and no per-gap attempt ledger; a stale pattern decays because no new signals have reinforced it, not because a counted number of directed probes returned void. The anchor needs to convert time-based decay into attempt-count-based decay: a gap that receives N agent-waves without signal-return should trigger the 'unfillable' halt, not a gap that has simply aged. This requires the cycle orchestrator to maintain an explicit attempt-ledger per gap, which stigmergy does not.


---

## Context 6 — domain: `medicine`  (report wander-S36c066-002)

**Original iteration-1 (what's being revised):**
> The source documents how designed scarcity — not model intelligence — is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wel


### SONNET (baseline, removed)  ($0.0149)
- **exploration:** The source documents how designed scarcity — not model intelligence — is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wellbeing is mean-reverting rather than accumulating, so local failures cannot cascade into permanent attractor hazard.
- **advancement:** The sharpest cross-world mapping is this: the anchor's orchestrator must prevent the wandering wave from crystallizing too early around a locally complete but globally distorted skeleton — the same failure mode the simulation solves by making scarcity renewable rather than exhaustible. Concretely: if the gap-ranker dispatches agents only toward the highest-priority gap each cycle, the skeleton risks a spoilage-equivalent — the lower-ranked gaps decay in coverage while the top gap gets saturated. The simulation's diet-variety constraint is structurally isomorphic to a dispatch rule that forces minimum coverage breadth per cycle, not just depth on the loudest gap. The mean-reversion wellbeing model maps even more precisely onto the anchor's 'bounded reversible bias injection' problem: recovery rate proportional to unmet need (not to history of failure) is exactly the shape a framing-distortion corrector needs — the twist injected during gap-chasing should pull back at a rate proportional to how much the current skeleton still deviates from structural balance, not proportional to how many cycles the twist has been running.
- **what_does_not_map:** The simulation's scarcity mechanics carry no attempt history and perform no self-diagnosis: a spoilage timer cannot distinguish 'this resource slot is empty because nothing exists here' from 'this resource slot is empty because no agent has visited it yet' — which is precisely the anchor's hardest open question (unfillable gap versus under-attempted gap). The source's dynamics are externally engineered and structurally static; the anchor's loop must read gap-type from inside its own attempt record, a capacity the simulation has no analog for.


### DEEPSEEK (new workhorse)  ($0.0044)
- **exploration:** The source demonstrates two structural patterns that sustain productive tension in a multi-agent simulation: (1) designed scarcity — diet variety, spoilage, fuel crisis — continuously regenerates pressure to trade, preventing the system from collapsing into a static equilibrium; (2) tolerant degradation via mean-reverting wellbeing ensures local failures never cascade, because wellbeing reverts toward a baseline rather than accumulating. Both are architectural choices about how to preserve divergence and bounded recovery.
- **advancement:** The 'designed scarcity' pattern sharpens the divergence-convergence tension in the anchor: instead of relying on smarter agents, an orchestrator could prevent premature convergence by a renewable attention scarcity — e.g., allocating fewer agents to a gap cluster the more it is chased, analogous to the fuel crisis forcing trade only when resources run thin. The second pattern offers a concrete mechanism for the anchor's 'bounded reversible bias injection': a framing distortion during gap-chasing could be governed by a mean-reverting decay, where the twist's influence fades at a rate proportional to how little the new information resolves the gap, ensuring the skeleton does not permanently warp from a temporary exploration bias.
- **what_does_not_map:** The source's scarcity and recovery mechanics are pre-designed environmental constants that never self-diagnose; they carry no attempt history and cannot distinguish an unfillable gap from an under-attempted one. The anchor must make that distinction by reading its own dispatch records — an epistemic, metacognitive jump that the simulation externalizes entirely onto the human architect, who set spoilage rates without any internal model of gap types.


### HAIKU (new nuance)  ($0.0054)
- **exploration:** The source documents how designed scarcity—not model intelligence—is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wellbeing is mean-reverting rather than accumulating, so local failures cannot cascade. These are structural choices about how a system sustains productive tension rather than collapsing to equilibrium.
- **advancement:** The 'designed scarcity as sustained divergence' pattern maps directly onto the anchor's mechanism-layer node: 'the structure diagnoses its own incompleteness, that diagnosis generates a biased sampling strategy.' In the source, scarcity is the diagnosis; the trade pressure is the biased sampling. But the anchor's loop must do this self-diagnostically—the gap-ranker and dispatcher must read attempt history to decide whether a gap is unfillable (agents sent, returned empty) versus under-attempted (agents never sent). The source's spoilage timer is a static external pressure; the anchor's gap-type classifier must be a learned attractor hazard—a state-space region where the system learns to recognize 'we have not looked here correctly' from the shape of failed attempts, not from a pre-set timer. This is the concrete mechanism the anchor needs: attempt-shape reading as the epistemic core of re-dispatch logic.
- **what_does_not_map:** The source's mean-reverting wellbeing model prevents accumulating failure by design—recovery rate is proportional to unmet need, not to history. But the anchor's core problem is the inverse: it must prevent a framing distortion (goal-aware bias injected during gap-chasing) from crystallizing into the skeleton permanently. Mean-reversion works when the system has a known attractor (wellbeing equilibrium); the anchor has no pre-set attractor—the skeleton itself is the attractor being built. A spoilage timer cannot read its own gap-type because it has no attempt history; the anchor's bounded-bias injection must be readable from the gap-ranker's own dispatch record. The source's tolerance (malformed outputs become no-ops) is passive; the anchor needs active negative topology—a mechanism that detects when goal-aware bias has over-constrained the skeleton and deliberately re-opens it.


---

**Total cost: $0.1674** · 18 calls
