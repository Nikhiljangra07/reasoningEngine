# Dig-quality A/B — Sonnet vs DeepSeek vs Haiku (revise step) — STRENGTHENED prompt

Source run: `20260616-171733` · 6 real contexts · same input, three models. **You judge the nuance.**

Read each block for: did it PRESERVE the cross-domain vocabulary and sharpen the analogy (good), or SAND it into generic prose (the failure mode the prompt warns against)?


---

## Context 1 — domain: `film`  (report wander-S589f28-001)

**Original iteration-1 (what's being revised):**
> Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its core architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts between agents rather than free-text handoffs. The pipeline stages — research, st


### SONNET (baseline, removed)  ($0.0158)
- **exploration:** Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its architectural core: frames as source-of-truth (continuity bugs caught at cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts as inter-agent contracts rather than free-text handoffs. The pipeline stages — research, structure, script, casting, set, director, storyboard, animator, continuity, editor — are fixed; what varies per genre is what each stage's typed artifact *means*, encoded in a genre template that acts as a schema overlay on the universal pipeline.
- **advancement:** Showrunner's typed artifact as inter-agent contract IS the anchor's bias-bounding membrane — and this predicts that the cycle orchestrator's gap-chasing distortion stays non-accumulating only if the re-integration step receives a schema-validated signal rather than the raw framing the gap-chasing agent operated under: specifically, a gap-chasing agent twisted toward a structural blind spot must emit a typed finding (e.g., 'structural piece of type X, confidence Y, provenance Z') so that the skeleton-reassembly stage works on normalized evidence, not on the agent's distorted goal-frame — exactly as Showrunner's continuity checker works on frame-diff arrays rather than on the director agent's narrative intent.
- **what_does_not_map:** Showrunner's pipeline is strictly acyclic — no downstream stage can re-open an upstream one, and there is no mechanism for accumulating attempt-history to discriminate 'unfillable gap' from 'under-attempted gap'; the anchor's loop requires precisely this: the orchestrator must read whether agents were dispatched at a gap and returned empty versus whether the gap was never attempted, a distinction Showrunner's typed artifact membrane cannot encode because a stage either produces its artifact or fails with no structured failure-provenance passed forward.


### DEEPSEEK (new workhorse)  ($0.0112)
- **exploration:** Showrunner is a production-pipeline-as-code framework for AI-generated multi-scene shows. Its core architectural bets are: frames as source-of-truth (continuity bugs caught at the cheap still-image stage before expensive video rendering), every agent as a pure function of (inputs, asset_registry), and typed artifacts between agents rather than free-text handoffs. The pipeline stages — research, structure, script, casting, set, director, storyboard, animator, continuity, editor — are fixed; what varies per genre is what each stage's typed artifact *means*.
- **advancement:** Showrunner's 'typed artifact as inter-agent contract' IS bounded reversible bias injection — because the typed output schema forces a framing-distorted agent's findings into a normalized, schema-validated signal before the downstream assembly stage, preventing the bias from accumulating into permanent structural distortion. This predicts that the cycle orchestrator can safely dispatch agents with twisted framings toward structural gaps as long as each agent returns a typed artifact whose schema guarantees clean re-integration.
- **what_does_not_map:** Showrunner's pipeline is unidirectional and linear: each stage's typed artifact is consumed by the next, and no mechanism allows a downstream stage to signal upstream that a structural gap remains unfilled and needs re-framing. The anchor's orchestrator requires a loop where dispatch decisions are based on attempt-history (whether agents were sent and came back empty) to distinguish unfillable from under-attempted gaps; Showrunner lacks an attempt-history discriminator entirely, treating failure as a dead end. Additionally, the typed artifact membrane that makes bias bounded also makes the pipeline rigid — there is no dual-signal fusion between goal-blind and goal-aware detectors, because the typed artifact is a single, normalized output, not a fusion of divergent signals.


### HAIKU (new nuance)  ($0.0052)
- **exploration:** Showrunner's pipeline stages are fixed; what varies per genre is the typed artifact schema each stage emits and consumes. Frames are the cheap commitment layer where continuity bugs surface before video rendering. Every agent is a pure function of (inputs, asset_registry), and the type contract between agents prevents framing distortion from one stage contaminating the next.
- **advancement:** Showrunner's typed artifact membrane IS the bounded reversible bias injection that the anchor's mechanism layer requires — when a gap-chasing agent twists its framing toward a structural blind spot, the type schema it must emit forces re-normalization before the next stage consumes it. This predicts that the cycle orchestrator can safely dispatch agents with goal-aware framing (the auditor's bias) and goal-blind framing (the structural finder's bias) into the same gap because the typed artifact handoff acts as the decontamination boundary: downstream re-integration does not inherit the agent's temporary framing distortion, only the normalized signal it produced. The frames-as-cheap-commitment layer also maps to the anchor's attempt-history discriminator — both systems resolve ambiguity (continuity bug or rendering artifact? unfillable gap or under-attempted gap?) at a representation layer where iteration is reversible before promoting to the expensive downstream commitment.
- **what_does_not_map:** Showrunner's pipeline cannot re-open upstream stages once a typed artifact is locked. The anchor's cycle orchestrator must hold simultaneous divergence and convergence: each cycle can twist framing, chase gaps, and fold findings back into the skeleton without a stage-ordering constraint. Showrunner has no dual-signal fusion mechanism — it does not accumulate evidence about *why* a stage failed to inform the next dispatch decision. The genre template varies the schema, but the schema is still a static contract; the anchor's gap-ranker must dynamically fuse a goal-blind structural piece-finder and a goal-aware auditor into a single dispatch priority, which requires the priority signal itself to be mutable across cycles, not just the artifact contents.


---

## Context 2 — domain: `relationships`  (report wander-S4f31ec-002)

**Original iteration-1 (what's being revised):**
> Stigmergy's ART-based competitive mesh offers a mature, theoretically grounded implementation of several mechanism-layer nodes: signals route via BFS stop-on-first-accept (satisficing under resource constraint), workers fork/merge/decay by familiarity threshold (convergence by saturation), and the vigilance parameter governs how aggressively new categories form versus reinforcing old ones (bounded


### SONNET (baseline, removed)  ($0.0158)
- **exploration:** Stigmergy's ART-based competitive mesh offers a theoretically grounded implementation of several mechanism-layer nodes: signals route via BFS stop-on-first-accept (satisficing under resource constraint), workers fork/merge/decay by familiarity threshold (convergence by saturation), and the vigilance parameter governs how aggressively new categories form versus reinforcing old ones (bounded reversible bias injection). The deeper find is ART's 'complement coding' — where full workers raise vigilance thresholds to prevent category monopoly — which is a structural guarantee, not a policy, that no single pattern consumes all routing capacity. This maps onto the anchor's specific fear that a high-priority gap, once identified by the auditor, starves the wandering wave by pulling all dispatch allocation toward itself.
- **advancement:** ART's vigilance parameter IS the anchor's 'bounded reversible bias injection' knob — and this predicts that the cycle orchestrator's gap-chasing intensity should be implemented not as a fixed agent-count rule but as a threshold on how much the current skeleton's gap-signal is allowed to distort the dispatch distribution before the system refuses to update and forces a free-wander cycle instead, preventing attractor collapse into a single gap.
- **what_does_not_map:** Stigmergy has no attempt-history discriminator: it cannot distinguish 'this gap is empty because nothing exists' from 'this gap is empty because routing was wrong' — stale patterns simply decay by age, which is exhaustion-based halting, not the saturation-based halting the anchor requires. More critically, the anchor's dual gap-detector fusion (goal-blind structural finder + goal-aware auditor operating in parallel without cross-contamination) has no counterpart in ART's architecture, which is uniformly goal-blind — so the contamination-prevention problem the anchor must solve is absent here, not resolved.


### DEEPSEEK (new workhorse)  ($0.0057)
- **exploration:** Stigmergy's ART-based competitive mesh remains the tightest mechanism-layer match in the room: 7/7 nodes map, with vigilance-gated plasticity as the mathematical implementation of 'bounded reversible bias injection' and complement coding as a structural guarantee against signal monopoly. The self-critique was right that the cross-world analogy needed a sharper joint — the revision finds it in ART's 'match tracking' loop, which is the precise dual of the anchor's gap-chasing cycle: a recursive self-amendment where each mismatch triggers a bias adjustment (vigilance raise) that alters the next sampling window, exactly as the anchor's gap detector biases the next agent wave.
- **advancement:** ART's match-tracking cycle — where a failed top-down expectation triggers a vigilance increment that shrinks the acceptance radius for the next bottom-up signal — IS the anchor's 'gap-chasing biases the next sampling window' mechanism, and this predicts that the cycle orchestrator can implement its gap-to-dispatch conversion as a vigilance delta rather than a separate routing policy: each structural gap raises the vigilance threshold on the category that claimed it, forcing the next agent wave to route findings into a different category or form a new one, which is a single-parameter implementation of 'twist its own framing toward an identified gap.'
- **what_does_not_map:** Stigmergy's mesh has no attempt-history discriminator — it cannot distinguish 'this gap is empty because nothing exists' from 'this gap is empty because I routed wrong'; stale patterns decay by age, which is exhaustion-based halting, not saturation-based. More critically, the anchor's dual gap-detector fusion (goal-blind structural finder + goal-aware auditor) has no counterpart here: stigmergy's mesh is uniformly goal-blind, surfacing whatever patterns emerge from signal statistics, with no second detector that knows what the draft needs — so the contamination-prevention architecture the anchor requires is absent, not solved.


### HAIKU (new nuance)  ($0.0056)
- **exploration:** Stigmergy's ART-based competitive mesh implements the mechanism layer's recursive self-amendment loop through three concrete nodes: BFS stop-on-first-accept routes signals under resource constraint (satisficing), workers fork/merge/decay by familiarity threshold (saturation-driven convergence), and vigilance gates plasticity to prevent category monopoly (bounded reversible bias injection). The tightest correspondence: ART's complement coding — where full workers raise vigilance thresholds to block further recruitment into saturated categories — is a structural implementation of goal-neutralization: it prevents any single gap-chasing dispatch from consuming the next wandering wave's routing capacity, which is the anchor's core allocation problem.
- **advancement:** ART's vigilance parameter IS the anchor's bias-injection governor — it tunes how aggressively new patterns (gaps) form versus reinforcing old ones (converged structure), and critically, it does so via a mathematical constraint (complement coding) rather than a policy choice. This predicts that the cycle orchestrator's allocation brain should not decide 'how much goal-awareness to inject' as a separate tuning problem, but instead should embed the goal-aware auditor's signal into a vigilance-like threshold that structurally prevents it from monopolizing the next dispatch wave, making contamination-prevention automatic rather than monitored.
- **what_does_not_map:** Stigmergy has no attempt-history discriminator: it cannot distinguish 'this gap returned empty because the gap is unfillable' from 'this gap returned empty because I routed the agents wrong.' Stale patterns decay by age alone, which is exhaustion-based halting. More critically, the anchor requires fusion of two independent detectors (goal-blind structural finder + goal-aware auditor) into one dispatch priority; stigmergy's mesh is uniformly goal-blind with no second detector that knows what the draft needs. The anchor's dual-signal fusion architecture — where goal-awareness must inform allocation without contaminating the structural finder — has no counterpart in stigmergy's single-signal routing.


---

## Context 3 — domain: `biographies`  (report wander-S63728f-001)

**Original iteration-1 (what's being revised):**
> The abryfs/organism source is a prompt-engineering coordination protocol for founder-engineers, not a self-amending research loop. Its nine 'organs' participate in or lead task flows via a fixed 7-step protocol, with tiered visibility and named pitfall detection. The high node-match count reflects structural vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the und


### SONNET (baseline, removed)  ($0.0149)
- **exploration:** The abryfs/organism source is a human-steered coordination protocol for founder-engineers: nine named organs participate in a fixed 7-step task flow, with tiered visibility, gate-override logging, and a 100-pitfall layer that fires Block/Warn/Note signals from attempt history. Its structural vocabulary — gap detection, convergence signals, multi-source fusion — overlaps heavily with the anchor's, but the underlying architecture is a human-triggered workflow, not an autonomous cycle that rewrites its own framing between iterations.
- **advancement:** The 'Eyes' organ's 100-pitfall layer — where each pitfall fires Block/Warn/Note conditioned on the history of prior gate overrides — IS the anchor's attempt-history-as-gap-type-discriminator operating in a human-in-the-loop substrate: the system distinguishes 'gap exists because the pattern is structurally absent' from 'gap exists because the founder is motivated to avoid it', and that distinction is only readable from the override log, not from the gap itself. This predicts a concrete design consequence: the cycle orchestrator's UNFILLABLE-vs-UNDER-ATTEMPTED classifier needs a third category — AVOIDED — readable only from whether agents were dispatched, returned non-empty, and the finding was nonetheless not integrated into the skeleton.
- **what_does_not_map:** In organism, 'Eyes' flags motivated-reasoning drift to the founder, who then decides whether to re-integrate or override — the distortion is never folded back into the structure autonomously. In the anchor's loop, the orchestrator must temporarily twist its own framing toward a gap and re-integrate what returns without any human gate; there is no analog in organism for a system that self-amends its framing and then self-evaluates whether the amendment closed the gap, because the founder owns both of those moves.


### DEEPSEEK (new workhorse)  ($0.0110)
- **exploration:** The abryfs/organism source is a human-steered prompt-engineering coordination protocol, not an autonomous self-amending research loop. Its nine 'organs' follow a fixed 7-step protocol with tiered visibility and a 100-pitfall layer that flags cognitive biases, self-deception, and drift. The high node-match counts reflect surface vocabulary overlap — gap detection, convergence signals, multi-source fusion — but the architecture is fundamentally a task-triggered workflow with a human founder closing every loop.
- **advancement:** The 100-pitfall layer's gate-override log — each pitfall firing as Block/Warn/Note from the founder's past decisions — IS the attempt-history-as-gap-type-discriminator: a record of whether a gap was reached and overridden versus never attempted, and this predicts the cycle orchestrator's halt/reattempt logic by reading whether agents were sent at a gap and returned empty, making the distinction between unfillable and under-attempted gaps internally readable.
- **what_does_not_map:** The source's gate-override log is human-facing, not system-facing; it does not autonomously adjust pitfall thresholds or re-route agents — the founder must interpret patterns and manually adjust the protocol. The anchor's attempt-history is read by the orchestrator itself to decide halt/reattempt, making the gap-fillability distinction a closed-loop internal mechanism. The convergence signals also differ: organism ends when the founder decides to ship, not when a structural completeness checkpoint is self-evaluated by the system.


### HAIKU (new nuance)  ($0.0053)
- **exploration:** The abryfs/organism source is a prompt-engineering coordination protocol where a human founder triggers cycles and confirms gates; its nine organs participate in fixed 7-step task flows with tiered visibility and named pitfall detection. The vocabulary overlap (gap detection, convergence signals, multi-source fusion) is real but reflects different architectures: organism is human-steered and task-triggered; the anchor requires autonomous cycle-closure and self-reframing without human re-entry.
- **advancement:** Organism's 'Eyes' organ operates as a goal-blind inward/outward bias detector that must remain contamination-free from the founder's own framing — this IS the anchor's 'fuse two independent gap-detectors (goal-blind structural finder + goal-aware auditor) without cross-contamination' problem, but solved at the human level rather than the system level. Eyes flags drift to the founder; the anchor's cycle orchestrator must flag drift to itself and re-integrate the corrected framing into the next dispatch without human re-entry. The gate-override log ('patterns reveal where judgment needs adjusting') IS an attempt-history discriminator that distinguishes 'gap because pattern absent' from 'gap because founder is avoiding it' — but organism uses this to surface patterns to the founder for manual threshold-tuning, whereas the anchor needs the system to auto-tune its own gap-chase allocation based on whether prior agent waves returned empty or returned-but-shallow.
- **what_does_not_map:** Organism has no halting criterion independent of the founder's decision to ship. The anchor's halting criterion is structural completeness the system itself evaluates against a checkpoint, or marginal returns collapse the system itself measures. Organism cannot temporarily twist its own framing (reweight which gaps matter, which agents to send) and then fold the result back into the skeleton without founder confirmation — every reframing is a human choice. The anchor requires the cycle orchestrator to do this autonomously: detect a gap, decide it's worth chasing, dispatch agents toward it, integrate findings, and decide whether to halt or loop — all without human steering between cycles.


---

## Context 4 — domain: `military_strategy`  (report wander-Seb9414-002)

**Original iteration-1 (what's being revised):**
> Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but the rest of the pipeline is a fixed assembly line, not a 


### SONNET (baseline, removed)  ($0.0151)
- **exploration:** Showspring is a 24-step linear production pipeline automating video creation through deterministic, sequenced stages — each step consuming the prior step's typed artifact toward a known terminal output (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but it is a one-shot tournament bracketed inside a fixed stage, not a recursive self-amendment loop. The pipeline knows its own shape before the first step runs.
- **advancement:** Showspring's Cooperative File Locks — preventing simultaneous AI engineering agents from corrupting shared git state — IS the anchor's 'bounded reversible bias injection' constraint at the artifact layer: both mechanisms solve the same problem (parallel agents must not permanently corrupt shared structure), but Showspring solves it by serializing writes to a fixed schema, while the anchor requires the constraint to operate on a *mutable* skeleton that is being structurally redefined each cycle. This predicts that the anchor's equivalent of file locking cannot be a simple mutex — it must be a versioned skeleton checkpoint that allows a gap-chasing agent wave to temporarily distort the shared structure while preserving the last coherent state for rollback if the distortion does not integrate.
- **what_does_not_map:** The specific discriminator the anchor needs — reading 'unfillable gap' versus 'under-attempted gap' from whether agents were actually dispatched at it and returned empty — has no counterpart in Showspring: every stage runs exactly once, so there is no attempt-history, no re-command-deeper logic, and no halting criterion based on structural saturation rather than pipeline exhaustion. The gap between these two is not a matter of degree; Showspring's architecture would require a conditional re-entry layer (something like LangGraph's looping edges with state-keyed guards) before any of the anchor's allocation logic could be instantiated inside it.


### DEEPSEEK (new workhorse)  ($0.0107)
- **exploration:** Showspring is a 24-step deterministic video generation pipeline; its most interesting features for the anchor are the hybrid compute routing (mapping each generation step to cloud or local RTX 5090 by a capability matrix) and the Cooperative File Locks that prevent conflicting writes from parallel AI agents onto a shared git state. Neither feature amounts to a self-amending loop, but together they surface allocation-by-structure and concurrency-consistency mechanisms that could be borrowed.
- **advancement:** Showspring's hybrid compute routing — where a static capability matrix assigns each generation step to the appropriate hardware class — IS the anchor's 'resource allocation from structural topology', because it replaces naive round-robin with structure-sensitive dispatch, and this predicts that the orchestrator can use a similar capability-gap matrix (e.g., allocating 1 agent for lexical gaps, 3 for cross-document structural gaps) to decide agent count per cycle from gap type rather than a fixed budget.
- **what_does_not_map:** The anchor demands that the pipeline itself can inject new steps mid-run when it diagnoses its own ignorance — a recursive self-amendment that bends its framing then re-integrates. Showspring's 24-step DAG is static; there is no mechanism for the pipeline to add a previously unplanned step, no attempt-history that discriminates 'nothing exists here' from 'I haven't looked right,' and no halting on structural saturation rather than on task completion. The 'divergence-convergence tension' match remains superficial: the Creative Director's one-shot persona tournament is a single-pass tournament, not a cycle that temporarily distorts framing and must be bounded and reversible.


### HAIKU (new nuance)  ($0.0059)
- **exploration:** Showspring is a 24-step linear production pipeline that automates video creation through sequenced, deterministic stages — each step consuming the output of the prior one toward a known artifact (a YouTube-ready MP4). The multi-model 'Creative Director' step (three personas + a judge) is the closest thing to divergence-then-convergence, but the rest of the pipeline is a fixed assembly line, not a self-amending loop. Critically: Showspring's halting criterion is completion of all 24 steps, not structural saturation or diminishing returns.
- **advancement:** Showspring's hybrid compute routing — sending tasks to cloud vs. local RTX 5090 based on capability requirements, not queue position — instantiates a *static* version of the anchor's gap-driven allocation brain: both systems route work by task topology rather than round-robin, but Showspring's topology is pre-computed (video codec complexity, model size) while the anchor's topology is *discovered during execution* (which structural piece is missing, how many agents have already failed at it). The Cooperative File Locks preventing conflicting git states across simultaneous AI engineering agents map to 'bounded reversible bias injection' only if you read them backward: they prevent corruption of shared state, but the anchor requires that bias *can* corrupt state temporarily and must be *deliberately re-integrated* — the lock is a safety brake, not a deliberate distortion mechanism. This reveals the sharpest joint: Showspring allocates compute by *static task signature*; the anchor must allocate agents by *dynamic gap signature* that changes as the structure self-amends.
- **what_does_not_map:** The anchor's recursive self-amendment loop — structure diagnoses incompleteness, diagnosis biases sampling, sampling returns evidence that opens new gaps, structure rebuilds — has no counterpart in Showspring. Showspring's Creative Director is a one-shot divergence-convergence (three personas generate, judge selects), not a cycle that re-enters with a modified framing based on what the prior cycle exposed. More critically: Showspring cannot distinguish between 'this gap is unfillable' and 'I have not yet sent the right agent at it' because it has no attempt-history layer and no gap-ranker that learns from agent-return patterns. The anchor's halting criterion is 'skeleton meets checkpoint OR marginal returns collapse'; Showspring's is 'all 24 steps complete.' These are orthogonal stopping rules.


---

## Context 5 — domain: `sociology`  (report wander-S4f31ec-001)

**Original iteration-1 (what's being revised):**
> Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a p


### SONNET (baseline, removed)  ($0.0156)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly.
- **advancement:** ART's vigilance parameter — the threshold controlling whether a new signal reinforces an existing category or forces a new one open — IS the anchor's gap-severity fusion dial: when vigilance is high, near-matches are rejected and new categories proliferate (analogous to the goal-blind structural finder treating every unmatched finding as a new gap); when vigilance is low, near-matches collapse into existing categories (analogous to the goal-aware auditor suppressing redundant gap-chasing). This predicts a concrete design move: the dual gap-detectors should not be fused by averaging their outputs but by running them at deliberately mismatched vigilance levels, then treating disagreement between them as the dispatch signal — a gap the auditor sees but the structural finder does not is an under-attempted gap worth chasing; a gap the structural finder sees but the auditor does not is a candidate unfillable gap worth flagging for halt.
- **what_does_not_map:** Stigmergy's decay is time-based and attempt-agnostic: a stale pattern fades because no new signal arrived, not because agents were sent at it and returned empty. The anchor's 'unfillable vs. under-attempted' discriminator requires attempt-count pressure per gap — a history of directed sends against a known structural target — which stigmergy's workers never accumulate, so the halting criterion cannot be read from stigmergy's state without grafting on an external attempt ledger that the stigmergic architecture has no native slot for.


### DEEPSEEK (new workhorse)  ($0.0123)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The vigilance parameter governs the sensitivity of gap-opening: when an incoming signal fails to match existing categories below a similarity threshold, a new category is forced open, providing a concrete mechanism for 'productive ignorance exposure'. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly.
- **advancement:** The ART vigilance parameter (which sets the similarity threshold for forcing a new category versus reinforcing an existing one) IS the anchor's fusion threshold for the two gap-detectors — and this predicts that the structural gap-finder and the goal-aware auditor can be fused by making the auditor's confidence modulate the vigilance parameter, so that gaps the auditor flags as critical lower the threshold for a new category to be spawned, effectively converting goal-relative blind spots into goal-free exploration leads without a separate priority list.
- **what_does_not_map:** Stigmergy has no explicit attempt-history per gap and no cycle orchestrator; its decay is time-based. The anchor's halting problem requires distinguishing unfillable gaps from under-attempted ones by tracking how many times agents have been sent at a specific structural target and returned empty. Stigmergy cannot provide this because its workers have no target structure and no memory of directed attempts — decay is time-based, not attempt-count-based, so the discrimination the anchor needs cannot be read from stigmergy's state.


### HAIKU (new nuance)  ($0.0055)
- **exploration:** Stigmergy uses Adaptive Resonance Theory to build a self-organizing mesh where signals from work artifacts compete for recognition: new patterns spawn new workers, familiar ones reinforce existing workers, stale ones decay — all without explicit retraining. The structural gap-detection (coordination gaps, knowledge silos, dependency risks) emerges from cross-source correlation rather than from a pre-specified schema. The mechanism is indirect coordination: agents leave traces that other agents respond to, rather than communicating directly.
- **advancement:** ART's vigilance parameter — which gates whether a new signal reinforces an existing category or forces a new one — IS the anchor's attempt-history pressure discriminator: both operate by accumulating unmatched signals until resonance fails and a new structural piece must be opened. But ART's vigilance is sensitivity-tuned (how similar must the signal be?), while the anchor's gap-ranker must be attempt-tuned (how many agent-waves have hit this gap empty?). The mapping holds for the BIAS-INJECTION half: reinforcing active workers injects bias toward familiar patterns, and decay bounds that bias. It breaks for the HALTING half: ART has no attempt-count memory per gap, so it cannot distinguish 'this gap is genuinely unfillable' from 'this gap was only probed once and came back empty.' The anchor needs attempt-history; stigmergy has only time-decay.
- **what_does_not_map:** Stigmergy is acyclic and target-free: workers respond to signals continuously, patterns surface as side-effects, and there is no skeleton being assembled toward a completeness checkpoint. The anchor's cycle orchestrator must decide agent-count per gap and halt when the skeleton satisfies a structural completeness criterion — both require a goal-relative target structure that stigmergy has no analogue for. Stigmergy's decay mechanism is time-based; the anchor's halting discriminator ('unfillable vs. under-attempted') requires attempt-count-based state that stigmergy does not track.


---

## Context 6 — domain: `medicine`  (report wander-S36c066-002)

**Original iteration-1 (what's being revised):**
> The source documents how designed scarcity — not model intelligence — is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wel


### SONNET (baseline, removed)  ($0.0147)
- **exploration:** The source documents how designed scarcity — not model intelligence — is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wellbeing is mean-reverting rather than accumulating, so local failures cannot cascade. These are structural choices about how a system sustains productive tension rather than collapsing to equilibrium.
- **advancement:** The simulation's mean-reverting wellbeing — where recovery rate is proportional to unmet need, not to accumulated failure history — IS the anchor's bounded-reversible-bias-injection mechanism: it is the concrete pattern for ensuring that a framing distortion injected during gap-chasing does not permanently warp the skeleton. This predicts a specific design choice: the orchestrator's twist-toward-gap should be parameterized by current gap magnitude alone, not by how many prior cycles failed to fill it, so that a persistently empty gap does not compound its own framing pressure into an attractor hazard that locks the skeleton into a distorted shape.
- **what_does_not_map:** The anchor's unfillable-vs-under-attempted distinction requires reading attempt history — the gap-type is only legible from whether agents were actually dispatched and returned empty. The simulation's scarcity mechanics are pre-designed by an external architect and carry no attempt history whatsoever; a spoilage timer cannot distinguish 'no food exists here' from 'no agent has looked here yet'. The self-amending loop the anchor needs is endogenous; the simulation's dynamics are exogenous and structurally static.


### DEEPSEEK (new workhorse)  ($0.0103)
- **exploration:** The source documents how designed scarcity—not model intelligence—is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. The system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wellbeing is mean-reverting rather than accumulating, so local failures cannot cascade. These are structural choices about sustaining productive tension rather than collapsing to equilibrium.
- **advancement:** The spoilage mechanic—which continuously reduces the usefulness of hoarded food regardless of consumption—IS the anchor's bounded reversible bias injection, because it demonstrates a self-limiting distortion that decays automatically, preventing permanent lock-in on a suboptimal allocation. This predicts an 'invalidation timer' design: the orchestrator could attach a freshness counter to each framing twist applied during gap-chasing, decaying it cycle by cycle unless new findings validate the gap's viability, thus preventing the skeleton from warping permanently toward a false structure.
- **what_does_not_map:** The source lacks attempt history and self-diagnosis: spoilage cannot distinguish an empty granary caused by spoilage (analogous to an unfillable gap) from one emptied by recent consumption (a gap successfully filled by agents). The anchor's epistemic tension requires reading gap types from how agents interacted with them; the simulation reads only resource levels, not the narrative of how they changed, so it cannot model the 'nothing exists' vs. 'not looked correctly' distinction.


### HAIKU (new nuance)  ($0.0057)
- **exploration:** The source documents how designed scarcity—not model intelligence—is the actual driver of emergent behavior in the Thousand Token Wood simulation. Three engineered constraints (diet variety, spoilage, fuel crisis) force continuous trade where a 'rational' system would have gone static. Separately, the system's robustness comes from tolerant degradation: malformed outputs become no-ops, and wellbeing is mean-reverting rather than accumulating, so local failures cannot cascade. These are structural choices about how a system sustains productive tension rather than collapsing to equilibrium.
- **advancement:** Designed scarcity in the simulation IS the anchor's 'bounded bias injection during gap-chasing'—both operate by deliberately constraining what the system can do in one cycle to force it to re-diagnose its own structure in the next. The spoilage timer does not let agents hoard; the cycle orchestrator's gap-ranker does not let the dispatcher send all agents at once. Both prevent premature convergence to a locally complete skeleton by making incompleteness renewable: in the simulation, scarcity regenerates each turn; in the orchestrator, a fresh gap-diagnosis arrives each cycle. This predicts that the orchestrator's allocation brain should treat 'under-attempted gap' not as 'send more agents' but as 'this gap regenerates because the skeleton is still incomplete'—the gap itself is the signal, not the agent count.
- **what_does_not_map:** The anchor's core tension is epistemic and self-correcting: the system must distinguish 'this gap is empty because nothing exists' from 'this gap is empty because I have not looked correctly', and that distinction is readable from attempt history and auditor disagreement. The source's scarcity mechanics are pre-designed by a human architect and static in structure; they carry no attempt history, no self-diagnosis, and no adaptive re-framing of what 'gap' means. A spoilage timer cannot read whether an agent was actually sent at a gap or whether the gap-ranker simply deprioritized it. The anchor needs to fuse goal-blind and goal-aware gap-detectors; the simulation has only one scarcity regime, externally imposed.


---

**Total cost: $0.1861** · 18 calls
